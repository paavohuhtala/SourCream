package paavohuh.sourcream.utils;

import org.jooq.lambda.Seq;
import org.jooq.lambda.tuple.Tuple2;

public class SeqUtils {
    public static <TL, TR> Seq<Tuple2<TL, TR>> cartesian(Seq<TL> left, Seq<TR> right) {
        return null;
    }
}
